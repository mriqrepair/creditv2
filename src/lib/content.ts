import { en } from "./i18n/en";

export const {
  company,
  navLinks,
  pricingPlans,
  services,
  differentiators,
  memberBenefits,
  testimonials,
  faqs,
  howItWorksSteps,
  educationArticles,
} = en;

export { getContent, DEFAULT_LOCALE, LOCALE_COOKIE, isLocale } from "./i18n";
export type { Locale, SiteContent } from "./i18n";
